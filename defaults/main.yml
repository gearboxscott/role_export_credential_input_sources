---
# defaults file for role_export_credential_input_sources

controller_validate_certs: false
controller_hostname: 'https://<name of controller>'
controller_username: admin
controller_password: CHANGEME

# Name of file to create that contains the exported credential input Sources

yaml_file: current_credential_input_sources.yml